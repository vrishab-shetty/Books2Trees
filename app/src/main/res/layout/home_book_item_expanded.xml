<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"

    android:layout_height="wrap_content"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/home_expanded_drag_down"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:padding="@dimen/expanded_view_padding">

        <requestFocus />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/home_expanded_text"
                style="@style/BottomSheetHeading" />

        </LinearLayout>

        <ImageView
            android:layout_width="30dip"
            android:layout_height="fill_parent"
            android:layout_gravity="end|center"
            android:layout_marginEnd="5dip"
            android:contentDescription="@string/home_expanded_hide"
            android:src="@drawable/ic_keyboard_arrow_down_24" />

    </FrameLayout>

    <com.company.books2trees.presentation.common.views.AutoFitRecyclerView
        android:id="@+id/home_expanded_recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:columnWidth="128dp" />

</LinearLayout>